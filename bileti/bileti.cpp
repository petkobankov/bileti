using namespace std;
#include <iostream>
#include "System.h"
const int LENGTH = 10;
int main()
{
	/*char command[LENGTH];

	cin >> command;
	System sys;


	while (strcmp(command, "exit") != 0) {

		if (strcmp(command, "open") == 0) {
			char location[1024];
			cin >> location;
			sys.open(location);
		}
		else if (strcmp(command, "DELETE") == 0) {
			int num;
			cin >> num;

			int index = test.find(num);
			test.erase(index);
		}
		else if (strcmp(command, "GET") == 0) {
			int index;
			cin >> index;

			int result = test.at(index);
			cout << result << endl;
		}
		else if (strcmp(command, "PRINT") == 0) {
			test.print();
		}
		else {
			cout << "Error" << endl;
			return 0;
		}

		cin >> command;

	}*/

}

